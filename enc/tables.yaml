Nodes:
    table_name: Nodes
    columns:
        fqdn:
            - TEXT
            - PRIMARY KEY
            - NOT NULL
        datacenter:
            - TEXT
            - NOT NULL
            - DEFAULT "default"
        cluster:
            - TEXT
            - NOT NULL
            - DEFAULT "default"
        role:
            - TEXT
            - NOT NULL
            - DEFAULT "default"
        environment:
            - TEXT
            - NOT NULL
            - DEFAULT "production"
